<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.planner.planner.user.mapper.userMapper">
	<select id="userSelect" resultType="com.planner.planner.DTO.RES.ResponseUserDTO" >
	
	SELECT 
		USERID,
		USERNAME,
		USERPHONE,
		USEREMAIL,
		ROLE,
		INSERTDATE
	FROM USER_MASTER
	
	</select>

</mapper>